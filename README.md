# pensieve-py38
To use this, first setup Docker on your computer, for windows- https://docs.docker.com/desktop/install/windows-install/

Then you can directly run ``eval.sh`` using  ``sh eval.sh`` and the results will be generated in the results folder.
